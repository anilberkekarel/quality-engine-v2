# QVM-V2 step 1 (data layer + raw viz). Signal-processing deps come later.
google-cloud-bigquery>=3.20
db-dtypes>=1.2          # BigQuery -> pandas dtype support
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
requests>=2.31          # only for the retired patentsview_provider (unused)
