# QVM-V2 steps 1-3 (data layers + raw viz + NB-HMM baseline).
google-cloud-bigquery>=3.20
db-dtypes>=1.2          # BigQuery -> pandas dtype support
pandas>=2.0
numpy>=1.24
scipy>=1.10             # NB-HMM: logsumexp, gammaln, dispersion optimization
matplotlib>=3.7
requests>=2.31          # SEC EDGAR XBRL fetches (financial channel)
yfinance>=1.0           # sanity cross-check of the SEC series (not a source)
hmmlearn>=0.3           # GaussianHMM cross-check ONLY (NB-HMM is ours)
